# U2a-R4 Batch 4 Closure / Batch 5-6 Execution Plan

Updated: 2026-08-27 22:20 CST
Repository: `slashinchi/TVBoxOS-Mobile` (public, default branch `patched`)
Baseline HEAD: `f6173586ea092669f8238519efd49e0fe7a13fbd`
Authority: U2a-R4 AUTHORIZED (D-036)

## Scope

Close Batch 4 residual evidence, then execute Batch 5 (control ref + authority
transition) and Batch 6 (final control-ref dispatch). No formal tag, GitHub
Release, `update.json` mutation, native remediation, or dependency change is
authorized.

## Batch 4 closure (already verified)

- Signed APK artifact `9647770098` from run `33075279552` downloaded locally;
  raw SHA-256 `ca279650fbec4915ab6d1d5e8161365708f33700a2420bee70f8f6c5f658c1f4`
  matches the attestation subject.
- `gh attestation verify` PASS for the default SLSA provenance v1 attestation
  with `--signer-workflow
  slashinchi/TVBoxOS-Mobile/.github/workflows/rc-pipeline.yml` and
  `--source-digest f6173586ea092669f8238519efd49e0fe7a13fbd`.
- `gh attestation verify` PASS for the custom
  `tvbox-release-identity/v2` attestation with the same signer and source
  digest. Verification JSON retained under
  `outputs/evidence/2026-08-27--u2a-batch4-attestation-verify/`.
- ACTIVE-PLAN header HEAD corrected to `f617358`.

## Batch 5 implementation

1. New `.github/workflows/rc-control.yml` (`workflow_dispatch` only):
   - `resolve` job: read-only `actions/github-script@v7.1.0` (full SHA pin)
     `getCommit('heads/patched')`; validates a 40-hex SHA; hard gate
     `github.ref == 'refs/tags/rc-control-v1' && github.actor == 'slashinchi'`.
   - `call_rc_pipeline` job: calls `./.github/workflows/rc-pipeline.yml`
     pinned by the control ref, `secrets: inherit`, both `release_sha` and
     `source_sha` set to the locked SHA, `mode: manual-local`.
2. `build.yml` cleanup:
   - remove `build-signed-rc` reusable-call wrapper (old patched-dispatch path);
   - remove `publish-github-release` legacy tag publisher;
   - remove `tags: ["v*"]` and `workflow_dispatch` triggers;
   - keep ordinary push/PR `build-apk` CI unchanged.
3. Contract tests (TDD, red first):
   - `test_rc_control_workflow_is_dispatch_gated_and_sha_locked`;
   - `test_build_workflow_has_no_signing_entry_and_no_tag_publisher`;
   - `test_signing_environment_is_limited_to_existing_signing_jobs` re-pointed
     at `rc-control.yml`.

## Batch 5 policy transition (fail-closed order)

Pre-save non-secret snapshots under
`outputs/evidence/2026-08-27--u2a-batch5-policy/`.

1. Ruleset `patched-r4-pr-checks` (branch, active): PR required (merge commit
   allowed), status check `build-apk`, `restrict_updates`,
   `restrict_deletions`, `blocks_force_pushes`, bypass: repository admin role.
2. Ruleset `rc-control-v1-immutable` (tag, active) matching exactly
   `refs/tags/rc-control-v1`: `restrict_updates`, `restrict_deletions`,
   `blocks_force_pushes`, no regular-actor bypass (repository admin only, as a
   documented residual risk).
3. Create lightweight tag `rc-control-v1` at the Batch-5-accepted HEAD
   (contains the control workflow) and push it.
4. Environment `release-signing`: add custom branch policy `rc-control-v1`
   (type `tag`), read back.
5. Delete the `patched` (branch) and `v*` (tag) environment policies, read back
   so exactly one tag policy remains.
6. Update environment protection: `reviewers: []` (removes required reviewer),
   keep `can_admins_bypass` as-is, never touch secret values (names not read).
7. Each step has a read-back assertion; any unexpected state stops with the
   saved snapshot for rollback.

## Rollback

Restore reviewer first (`PUT` with saved reviewer config), restore `patched` +
`v*` environment policies, delete tag policy/rulesets/tag only in reverse
order, revert code by normal commits. Never export/replace secrets, never
force-push, never delete formal releases.

## Batch 6 final acceptance

- Capture `gh workflow run rc-control.yml --repo ... --ref rc-control-v1`
  timestamp, then `gh run list` filter by `createdAt` to get the run.
- No approval wait occurs; full digest chain agrees: control tag commit,
  locked `patched` SHA, builder identities, signer input, verifier subject,
  attestation subject; artifact contracts exactly 6/2/2/2 (plus comparison
  report); reproducibility status `equivalent`.
- Both attestations re-verify with `gh attestation verify` and
  `--signer-workflow ...rc-pipeline.yml --source-digest <tag-sha>`.
- Confirm zero formal side effects: no new `v*` tag, no Release, `update.json`
  unchanged, `git ls-remote` before/after diff is only `rc-control-v1`.
- Retention policy: keep key success/diagnostic runs and evidence; do not
  batch-delete; note expiries in timeline.

## Closeout

Append FACTS, update ACTIVE-PLAN (Batch 5/6 DONE), rewrite HANDOFF to
`REVIEW_PENDING`, upload all changed Drive docs with read-back, then stop for
independent goal-level closeout.
